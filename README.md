# Cost ADR Visualizer

Interactive browser visualization for FSRS Cost ADR policies.

```sh
pnpm install
pnpm dev
```

Paste a `CostAdrPolicy` JSON object from `fsrs-rs`, adjust `cost weight`, and inspect the resulting Adaptive Desired Retention surface over stability and difficulty.
